#include "main.h"
/**
 * main - point of entry
 * @argc: is the argument count
 * @argv: is the argument vector
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	if (argc == 1)
	{
		puts(argv[0]);
	} return (0);
}
