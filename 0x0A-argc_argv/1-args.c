#include "main.h"
#include "stdio.h"
/**
 * main - entry point
 * @argv:is the argument passed
 * @argc:is the number of arguments passed
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	if (argv != NULL)
	{
		printf("%d\n", argc);
	} return (0);
}
