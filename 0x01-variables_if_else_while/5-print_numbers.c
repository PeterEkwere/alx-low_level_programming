#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0
 *
 * Return:Always 0 success
 */
int main(void)
{
	for (int i = 0; i < 10; i++)
		printf("%i", i);
	printf("\n");
	return (0);
}

